<!-- Generated by agent-context-sync. Do not edit directly; capture changes through the sync workflow. -->
<!-- Workspace ID: ws_01J00000000000000000000000 -->
<!-- Context commit: abc123def456 -->
<!-- Content hash: sha256:f412955c75f03387174211f4358878654beee591ec2306b2f639e869115ee631 -->

# Agent guidance

## Workspace

- Prefer pnpm for installs across the workspace.
  - Reason: Keeps lockfiles consistent.

## Repository

- API handlers must validate input at the boundary.
  - Reason: Prevents untrusted data from reaching domain logic.

## Path-specific

- Auth module guidance.

Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only. Keep session tokens server-side only.
  - Reason: Path-scoped auth rules are detailed.

## Agent-specific

- Prefer nested AGENTS.md over oversized root files.
  - Reason: Respects Codex project-doc size limits.

## Active work

- Migrating auth session storage to Redis.
  - Reason: Active cutover work.
